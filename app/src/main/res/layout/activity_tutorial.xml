<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/constraintLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/light_yellow">

    <TextView
        android:id="@+id/textChat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to MindMate!\nThis is a tutorial."
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="40dp"
        android:gravity="center" />

    <TextView
        android:id="@+id/tutorialText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="In this tutorial, you'll learn how to use the MindMate app.\n\nStep 1: You have just registered with your TU/e email and will receive a verification email. wait for it and click on the verification link and click on the link\nStep 2: Now you can Log in with your email and password\nStep 3: Now you can start matching with other students using the middle page!\nOther Steps: Chatting on the left, Profile (settings) on the right\n\nTap 'Close Tutorial' to get started."
        android:textSize="16sp"
        android:textColor="@android:color/black"
        app:layout_constraintTop_toBottomOf="@id/textChat"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="40dp"
        android:padding="16dp"
        android:gravity="center" />

    <Button
        android:id="@+id/buttonCloseTutorial"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:backgroundTint="#D32F2F"
        android:text="Close Tutorial"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/tutorialText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <ImageView
        android:id="@+id/logo"
        android:layout_width="185dp"
        android:layout_height="201dp"
        android:layout_marginTop="512dp"
        android:layout_marginEnd="112dp"
        android:contentDescription="MindMates Logo"
        android:src="@drawable/mindmates_logo_with_text_512x"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
